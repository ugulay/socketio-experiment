enum BOT_EVENTS {
    init = "init",
    connect = "connect",
    disconnect = "disconnect",
    reconnect = "reconnect",
    restart = "restart",
    update = "update",
    error = "error"
}

export {
    BOT_EVENTS
}